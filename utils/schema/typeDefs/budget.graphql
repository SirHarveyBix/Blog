# TODO : amount will be a currency
# appear this way : length(unit: LengthUnit = METER): Float

type Budget {
  id: ID
  amount: Int
  label: String
  author: [User]
}
input BudgetInput {
  amount: Int
  label: String
  author: [UsersBudgetInput]
}

input UsersBudgetInput {
  # id: ID
  email: String
}

type Query {
  getAllBudget(data: FindUserEmail): [Budget]
}
type Mutation {
  createBudgetLine(data: BudgetInput): Budget
}
